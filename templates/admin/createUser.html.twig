{% extends 'admin/index.html.twig' %}


{% block title %} User registre {% endblock %}

{% block body %}
    <h3>Section pour ajouter un utilisateur</h3>
    <br/>
    <br/>
    {{ form_start(formUser) }}

    {{ form_widget(formUser) }}
    <button type="submit" class="btn btn-primary">Enregistrer</button>
    {{ form_end(formUser) }}
{% endblock %}
